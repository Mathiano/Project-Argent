# THE ROAD (post-UI, confirmed by Mathias)
State: combat CLOSED. Battle UI v2 DONE at the green line (NOTE: beat 3.2 needs its commit+push - it exists only on CC's machine). Catalog: design+engine+CH1+trainer-Calls done; Reactive remains.

## The sequence
1. **REACTIVE (catalog part B)** - the last foundation. The adaptivity knob goes live: Reactive profiles read the player's stance history (the SAME array THROW THEM OFF poisons - the counterplay shipped before the threat) and counter-weight their picks. Two levels per catalog (reactive-lite mid / full elite). Closes the catalog; every Gym-2+ trainer can express its spec; the espionage pair gets real opponents.
2. **ANIMATION RUNTIME + PROOF BATCH** - both banked dependencies met (combat locked, 640 UI + stage done). CC builds the timeline-JSON interpreter on gameEvents + the preview harness; then a 3-5 animation proof batch (hit flash, HP drain, star pop, battle-enter wipe), Mathias feel-gates in the harness. Validates the CD->JSON->harness pipeline BEFORE choreography scale.
3. **GYM 2 VERTICAL SLICE** - the content era opens. One complete slice: CH2 area decisions (world-scope biome/classes), the CH2 mon batch (first Drainer + Pacer mons; MURKIN the gym ace; pre-spec'd stamina), gym trainers stamped from the catalog, the leader's bespoke overlay + boss card. Every content pipeline exercised once - THEN mass production.

## Parallel / banked
- Font exploration pass (vendor m5x7 + CC0 candidates, render the real screen in each, eye-pick) - optional polish, anytime.
- Stage-7 bond design (Mathias's) - rewards waiting.
- PLAYTEST DEBT: the info-war pair (READ THEM / THROW THEM OFF) vs KAMON has no verdict yet.
- Trainer-side info-war Call usage (a Reactive follow-up, not in the Reactive increment).
- Terrain dimension (Layer-3/arena work) - AMBUSHER waits.
- Reader-toolset upgrade re-baseline (post-content-push) - deliberate, dated event.
- Espionage verdict (Mathias): THROW THEM OFF accepted as a human combination-tool bots structurally under-measure; the coordinated-exploit bot folds into the banked reader-toolset upgrade event.
- Event stream -> Daily Puzzle (post-core marquee) - Shake It Off SHIPPED (in the Calls increment).
